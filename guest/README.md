# Guest

Future Linux appliance materials belong here: the Alpine-based prototype image, guest agent, `dm-cache` assembly, and LIO configuration.

The guest is intended to be a narrow storage appliance, not a general-purpose server. No appliance is built in B0.

