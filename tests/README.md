# Tests

Cross-component and storage safety tests will live here. Destructive tests must use explicitly disposable media and must never default to a real or system disk. B0 includes no destructive test harness.

